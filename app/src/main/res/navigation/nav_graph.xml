<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/creaturesListFragment">

    <fragment
        android:id="@+id/creaturesListFragment"
        android:name="com.demo.android.mapapp.view.fragment.CreaturesListFragment"
        android:label="@string/app_name">
        <action
            android:id="@+id/action_creaturesListFragment_to_mapFragment"
            app:destination="@id/mapFragment" />
    </fragment>
    <fragment
        android:id="@+id/mapFragment"
        android:name="com.demo.android.mapapp.view.fragment.MapFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_maps" />
</navigation>